import React from "react";

export default function App() {
    return (
        <div className="container">
            <div className="content-container">
                {"Rest Timer App - " + new Date()}
            </div>
        </div>
    );
}